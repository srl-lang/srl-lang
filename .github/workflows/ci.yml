name: SRL Language CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build SRL Executable
        run: cmake --build build --config Release

      - name: Run Standard Library Tests (Windows)
        if: runner.os == 'Windows'
        run: |
          .\build\Release\srl.exe run examples/test_libs.srl
          .\build\Release\srl.exe run examples/test_cpp_features.srl
          .\build\Release\srl.exe test examples/math.test.srl
          .\build\Release\srl.exe test examples/test_operator_overload.srl
          .\build\Release\srl.exe test examples/test_access_control.srl
          .\build\Release\srl.exe test examples/test_exceptions.srl
          .\build\Release\srl.exe test examples/test_generics.srl

      - name: Run Standard Library Tests (Linux)
        if: runner.os == 'Linux'
        run: |
          ./build/srl run examples/test_libs.srl
          ./build/srl run examples/test_cpp_features.srl
          ./build/srl test examples/math.test.srl
          ./build/srl test examples/test_operator_overload.srl
          ./build/srl test examples/test_access_control.srl
          ./build/srl test examples/test_exceptions.srl
          ./build/srl test examples/test_generics.srl
